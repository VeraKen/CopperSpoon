THE COPPER SPOON — SIGN UP, LOGIN & GOOGLE AUTH SETUP

1. Create a free project at https://supabase.com
2. In Supabase, open Project Settings > API.
3. Copy the Project URL and the public anon key.
4. In Vercel > Environment Variables add:
   NEXT_PUBLIC_SUPABASE_URL = your Supabase Project URL
   NEXT_PUBLIC_SUPABASE_ANON_KEY = your Supabase public anon key
5. Apply both to Production and Preview, then redeploy.

EMAIL AND PASSWORD
- In Supabase > Authentication > Providers, keep Email enabled.
- In Authentication > URL Configuration set Site URL to https://thecopperspoon.com
- Add these Redirect URLs:
  https://thecopperspoon.com/**
  https://copper-spoon.vercel.app/**

AUTH EMAIL DELIVERY (PREVENT "EMAIL RATE LIMIT EXCEEDED")
The Supabase test email service is limited to two messages per hour. Connect the
verified Copper Spoon Resend domain as custom SMTP for a public website.

1. In Supabase open Authentication > Emails > SMTP Settings.
2. Enable Custom SMTP and enter:
   Sender email: account@send.thecopperspoon.com
   Sender name: The Copper Spoon
   Host: smtp.resend.com
   Port: 465
   Username: resend
   Password: a Resend API key (create a separate key for Supabase Auth)
3. Save. Then open Authentication > Rate Limits and choose an appropriate
   email-sending limit. Start with 25 per hour unless the site needs more.
4. Test one new signup and check the Resend Emails page if delivery fails.

The Resend domain must show Verified before using this sender. Never add the
SMTP password to NEXT_PUBLIC variables or commit it to this ZIP/GitHub.

GOOGLE SIGN-IN
1. In Google Cloud Console create OAuth 2.0 Web credentials.
2. Copy the callback URL shown in Supabase > Authentication > Providers > Google.
3. Add that URL as an Authorized redirect URI in Google Cloud.
4. Copy the Google Client ID and Client Secret into the Supabase Google provider and enable it.

Never put the Supabase service-role key in the browser or in NEXT_PUBLIC variables.
